name: vizor
description: A new Flutter package project.
version: 0.0.7
homepage: https://github.com/vizor-ui/vizor

environment:
  sdk: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  soundpool: ^2.3.0-nullsafety

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/sounds/
